import { createCss } from "@tokenami/css";

// eslint-disable-next-line @typescript-eslint/no-restricted-imports -- allowed for now
import config from "../../.tokenami/tokenami.config.js";

export const css = createCss(config);
